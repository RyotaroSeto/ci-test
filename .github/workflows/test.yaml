name: Test

on:
  push:
    branches:
      - main
    paths:
      - "/test/1.txt"
      - "/test/2.txt"
      - "/test/3.txt"
  workflow_dispatch:

jobs:
  call_reusable_workflow:
    uses: ./.github/workflows/reusable.yaml
    with:
      test: hello1
